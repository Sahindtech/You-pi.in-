# App ka naam
spring.application.name=youpi

# ? Render PostgreSQL Database Connection Settings (FINAL FIX)
# Hum ab poora JDBC URL seedha Render se le rahe hain
spring.datasource.url=${DB_URL}

# ? Hibernate ko batayein ki hum PostgreSQL use kar rahe hain
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Baaki JPA/Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true


#spring.application.name=youpi
#spring.datasource.url=jdbc:mysql://localhost:3306/youpi
#spring.datasource.username=root
#spring.datasource.password=
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#
## JPA / Hibernate settings
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
